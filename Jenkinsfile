
pipeline {
    agent {label 'Jenkins-slave'}
    
    stages {
        stage ('Initialize') {
            steps {
                sh '''
                    echo "radhey"
                   
                '''
            }
        }

        stage ('Build') {
            steps {
                sh '''
                echo "radhey"
                '''
            }
        }
    }
}
